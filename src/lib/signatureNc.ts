export async function initSignature({ projectId }: { projectId: string }) {
  // Placeholder: call Signature.nc API to create a signature request for the project's documents
  // Use SIGNATURENC_API_BASE and SIGNATURENC_API_KEY
  return { ok: true, projectId, signatureRequestId: "demo-sign-1" };
}
